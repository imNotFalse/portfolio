services:
  # Uncomment when you're ready to add a database
  # db:
  #   image: postgres:17
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: portfolio
  #     POSTGRES_PASSWORD: portfolio
  #     POSTGRES_DB: portfolio
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
